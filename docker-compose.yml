version: '3.8'

services:
  backend:
    build: ./backend
    container_name: college-lens-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: college-lens-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
